# Copiar a .env (no se commitea). MS2 — Vuelos / Operaciones.

PORT=8002
ENV=local

# PostgreSQL 16
DB_HOST=localhost
DB_PORT=5432
DB_NAME=vuelos
DB_USER=postgres
DB_PASSWORD=changeme

# JVM ajustada para t3.small (ver MS2-10 / riesgo R7)
JAVA_OPTS=-Xmx256m -XX:MaxRAMPercentage=75 -XX:+UseSerialGC
